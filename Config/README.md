# Steps to generate input, output, cpp and readme file on linux

## Configuration steps

-  `chmod +x ./install.sh`
- `./install.sh`

## How to generate folder named add_two_numbers
- `mkqdir add_two_numbers`